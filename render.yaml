services:
  # Python Backend (FastAPI with Docker for OCR/Vision)
  - type: web
    name: xylo-backend
    env: docker
    plan: free
    envVars:
      - key: PROJECT_NAME
        value: "XYLO AI Invoice System"

  # Next.js Frontend
  - type: web
    name: xylo-frontend
    env: node
    plan: free
    buildCommand: npm install && npm run build
    startCommand: npm start
    envVars:
      - key: NEXT_PUBLIC_API_URL
        value: https://xylo-backend.onrender.com
      - key: NODE_VERSION
        value: 20.x

